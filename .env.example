# Copy to `.env` for local overrides. Mode templates use a blank API host so CI can inject `VITE_ENGAGE_API_HOST` from GitHub secrets.

VITE_APP_NAME=Clarus Engage
VITE_APP_ENV=development

VITE_ENGAGE_API_HOST=
