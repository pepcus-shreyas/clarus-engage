# Loaded for `vite` and `vite build --mode development`.
# Host is blank so CI injects `VITE_ENGAGE_API_HOST` from GitHub secrets. For local `npm run dev`, set the host in `.env.development.local`.

VITE_APP_NAME=Clarus Engage
VITE_APP_ENV=development

VITE_ENGAGE_API_HOST=
